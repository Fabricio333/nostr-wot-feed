# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A Nostr Web of Trust (WoT) feed application. This is an early-stage Node.js project.

## Commands

- **Run:** `node index.js`
- **Tests:** No test framework configured yet (`npm test` is a placeholder).

## Architecture

- **Entry point:** `index.js`
- **Package manager:** npm (no dependencies installed yet)
- **IDE:** WebStorm (`.idea/` directory present)
