{
  "name": "untitlednostr-wot-feed",
  "version": "1.0.0",
  "description": "Nostr Web of Trust feed client",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "^4.21.0",
    "nostr-tools": "^2.10.0"
  }
}
